[
  {"query": "the check-doc-examples job just went red on my PR — it says `cannot find 'trustedDeviceId' in scope` in the new mfaDelete example I added. what's the move?", "should_trigger": true},
  {"query": "I added a snippet under docs/modules/ROOT/examples for the new passkey signup and docs/verification/check.sh fails with 'cannot convert value of type String to expected argument type URL'. is that a real doc bug or do I need to touch the harness?", "should_trigger": true},
  {"query": "can you add a fixture for MyPaymentSDK to the doc examples verification setup? a new custom-provider example references it", "should_trigger": true},
  {"query": "I renamed a parameter on refreshAccessToken in Sources/Core and now the doc example compile check is complaining. can you reconcile it?", "should_trigger": true},
  {"query": "how do I regenerate the baseline for the documentation examples API check after fixing a couple of the broken snippets?", "should_trigger": true},
  {"query": "new example uses `self` as the presentation context and the DocExamples target won't build — the error is about .Type not conforming to ASWebAuthenticationPresentationContextProviding", "should_trigger": true},
  {"query": "docs/verification/check.sh is failing on registerCustomProvider with a bunch of 'cannot find' errors, can you sort out whether it's the harness or the doc?", "should_trigger": true},
  {"query": "we shipped a WeChat provider example and the doc-examples build says the provider type isn't found. how should the harness deal with provider-pod examples?", "should_trigger": true},
  {"query": "one of the doc example checks fails with 'missing argument for parameter redirectUri' — should I add it to the baseline or is the harness missing a fixture?", "should_trigger": true},
  {"query": "the API example verification broke on my branch after an SDK change, walk me through deciding whether to fix the harness or the docs", "should_trigger": true},
  {"query": "the ReachFive logout example in the docs reads awkwardly — can you rewrite the surrounding AsciiDoc prose to explain token revocation better?", "should_trigger": false},
  {"query": "my Sandbox app won't launch in the simulator, there's a UIKit crash in AppDelegate at startup", "should_trigger": false},
  {"query": "run the Reach5Tests XCTest suite and summarize which tests fail", "should_trigger": false},
  {"query": "the CircleCI `build` job is failing on `pod lib lint Reach5.podspec`, can you look at the podspec?", "should_trigger": false},
  {"query": "there's a compile error in my SwiftUI view: `cannot find 'viewModel' in scope` — I think I forgot an @StateObject", "should_trigger": false},
  {"query": "refresh the fixtures in my XCTestCase setUp — the mock AuthToken is stale", "should_trigger": false},
  {"query": "update the reference snapshots for our snapshot tests, the UI changed a bit", "should_trigger": false},
  {"query": "write a brand-new AsciiDoc page documenting the MFA step-up flow with a Swift code sample", "should_trigger": false},
  {"query": "generate the API reference docs from the /// doc comments in Sources/Core", "should_trigger": false},
  {"query": "how do I add a Google provider to the Sandbox app's AppDelegate?", "should_trigger": false}
]
